<% provide(:title, "Sign in") %>
<h1>Sign in</h1>

<%= cw_form_for :session, url: sessions_path do |f| %>
	<%= f.text_field :mail, label: 'Email' %>
	<%= f.password_field :password %>
	<%= f.submit 'Sign in' %>
<% end %>
<p>New user? <%= link_to "Sign up now!", signup_path %></p>
