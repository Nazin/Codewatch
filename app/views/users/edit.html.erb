<% provide(:title, 'Edit user') %>
<h1>Update your profile</h1>

<%= cw_form_for @user, url: user_edit_path do |f| %>
	<%= f.text_field :mail, label: 'Email' %>
	<%= f.text_field :name, label: 'Name' %>
	<%= f.submit "Save changes" %>
<% end %>
