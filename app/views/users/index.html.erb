<% provide :title, 'All users' %>

<% if has_role? UserCompany::ROLE_OWNER, false %>
	<%=link_to "Invite new user", users_invite_path, :class => 'button r' %>
<% end %>

<h1>All users</h1>

<ul class="users">
	<%= render @users %>
</ul>
